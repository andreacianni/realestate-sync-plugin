# 📊 DIAGRAMMA DEI FLUSSI DI IMPORT

## 🎯 SITUAZIONE ATTUALE (CAOTICA)

```
                             ENTRY POINTS
                                  │
          ┌───────────────────────┼───────────────────────┬────────────────┐
          │                       │                       │                │
    [Manual Import]         [WP Cron Daily]      [Server Cron]      [Test Upload]
     Button Click           Scheduled Daily        Every Minute       Upload File
          │                       │                       │                │
          │                       │                       │                │
          ▼                       ▼                       ▼                ▼
   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐   ┌─────────────┐
   │ AJAX Action │         │  WP  Hook   │         │  PHP File   │   │ AJAX Action │
   │  manual_    │         │   daily_    │         │  batch-     │   │  import_    │
   │  import     │         │   import    │         │  continuation│  │  test_file  │
   └─────────────┘         └─────────────┘         └─────────────┘   └─────────────┘
          │                       │                       │                │
          │                       │                       │                │
    ⚠️ DUPLICATO!                │                       │                │
          │                       │                       │                │
      ┌───┴───┐                   │                       │                │
      │       │                   │                       │                │
      ▼       ▼                   ▼                       ▼                ▼
  ┌────┐   ┌────┐             ┌────┐                 ┌────┐           ┌────┐
  │ A  │   │ B  │             │ C  │                 │ D  │           │ E  │
  │Admin│  │Main│             │Main│                 │Batch│          │Admin│
  │Class│  │File│             │File│                 │Cont│           │Class│
  └────┘   └────┘             └────┘                 └────┘           └────┘
     │       │                   │                       │                │
     │       │                   │                       │                │
     ▼       ▼                   ▼                       │                ▼
  ┌──────┐ ┌──────┐          ┌──────┐                   │             ┌──────┐
  │BATCH │ │CHUNKED│         │CHUNKED│                  │             │CHUNKED│
  │SYSTEM│ │IMPORT │          │IMPORT │                  ▼             │IMPORT │
  └──────┘ └──────┘          └──────┘              ┌──────────┐      └──────┘
     │       │                   │                  │  BATCH   │          │
     │       │                   │                  │  SYSTEM  │          │
     ▼       ▼                   ▼                  └──────────┘          ▼
  ┌──────────────────────────────────────────────────────────────────────────┐
  │                          IMPORT EXECUTION                                 │
  └──────────────────────────────────────────────────────────────────────────┘
```

**🚨 PROBLEMI VISIBILI:**
1. Manual Import ha DUE handler (A e B)
2. Sistema BATCH usato solo da A e D
3. Sistema CHUNKED usato da B, C, E (60%)
4. Confusione su quale handler viene eseguito

---

## ✅ BATCH SYSTEM (Corretto ma usato solo 40%)

```
┌─────────────────────────────────────────────────────────────────┐
│                      BATCH ORCHESTRATOR                         │
│                  (process_xml_batch)                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
   ┌────────┐      ┌─────────┐     ┌──────────┐
   │ STEP 1 │      │ STEP 2  │     │ STEP 3   │
   │ Index &│      │ Create  │     │ Process  │
   │ Filter │      │ Queue   │     │ 1st Batch│
   └────────┘      └─────────┘     └──────────┘
        │                │                │
        ▼                ▼                ▼
   ┌─────────────────────────────────────────┐
   │   Filter 28,625 → 781 properties        │
   │   (comune_istat 021/022 only)           │
   └─────────────────────────────────────────┘
        │                │                │
        ▼                ▼                ▼
   ┌─────────────────────────────────────────┐
   │   Queue Table (kre_realestate_         │
   │   import_queue)                         │
   │   - 30 agencies                         │
   │   - 781 properties                      │
   └─────────────────────────────────────────┘
        │                │                │
        ▼                ▼                ▼
   ┌─────────────────────────────────────────┐
   │   BATCH PROCESSOR                       │
   │   - Get 10 items from queue             │
   │   - Process each via Import Engine      │
   │   - Timeout: 50 seconds                 │
   └─────────────────────────────────────────┘
                         │
                         ▼
   ┌─────────────────────────────────────────┐
   │   CONTINUATION                          │
   │   - Set transient if not complete       │
   │   - Cron picks up and continues         │
   └─────────────────────────────────────────┘

LOGGING:
  [BATCH-ORCHESTRATOR] → debug.log
  [BATCH-PROCESSOR] → debug.log
  [BATCH-CONTINUATION] → debug.log
```

**✅ VANTAGGI:**
- Filtra PRIMA (solo 781 items processati)
- Queue persistente in database
- Batch processing (10 items/batch)
- Auto-continuation via cron
- Robusto a timeout/crash

**❌ SVANTAGGI ATTUALI:**
- Usato solo da 2/5 entry points
- Logging sparso in debug.log
- Batch size fisso (10)

---

## ❌ CHUNKED IMPORT (Vecchio - usato 60%)

```
┌─────────────────────────────────────────────────────────────────┐
│               IMPORT ENGINE                                     │
│          (execute_chunked_import)                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
   ┌─────────────────────────────────────────┐
   │   STREAMING PARSER                      │
   │   - Read XML chunk by chunk             │
   │   - NO pre-filtering                    │
   │   - Process ALL 28,625 properties       │
   └─────────────────────────────────────────┘
                         │
                         ▼
   ┌─────────────────────────────────────────┐
   │   handle_single_property()              │
   │   For EACH property:                    │
   │   ├─ Check deleted                      │
   │   ├─ Province filter (DURING)           │
   │   │   └─ 10,341 SKIPPED!                │
   │   └─ Process if passed                  │
   └─────────────────────────────────────────┘
                         │
                         ▼
   ┌─────────────────────────────────────────┐
   │   RESULT                                │
   │   - Processed: 10,556 properties        │
   │   - Skipped: 10,341 (province filter)   │
   │   - Deleted: 7,728 (marked deleted)     │
   │   - Created: 132 (rest failed/timeout)  │
   └─────────────────────────────────────────┘

LOGGING:
  import-{timestamp}-{session}.log
  ✅ STEP 1: Property Processing Started
  ❌ STEP 1b: SKIPPED - province filtering failed
  ✅ STEP 1c: Province filtering passed
  ... etc
```

**❌ PROBLEMI:**
- Processa TUTTE le 28,625 proprietà
- Filtra DURANTE invece che PRIMA
- Spreco enorme di risorse
- NO queue, NO continuation
- Timeout su proprietà con tante immagini
- Log in file separato

**⚠️ USATO DA:**
- Manual Import (handler main file)
- Scheduled Daily Import
- Test File Upload
- Test Sample XML

---

## 🎯 SITUAZIONE DESIDERATA (PULITA)

```
                             ENTRY POINTS
                                  │
          ┌───────────────────────┼───────────────────────┬────────────────┐
          │                       │                       │                │
    [Manual Import]         [WP Cron Daily]      [Server Cron]      [Test Upload]
     Button Click           Scheduled Daily        Every Minute       Upload File
          │                       │                       │                │
          ▼                       ▼                       ▼                ▼
   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐   ┌─────────────┐
   │ AJAX Action │         │  WP  Hook   │         │  PHP File   │   │ AJAX Action │
   │  manual_    │         │   daily_    │         │  batch-     │   │  test_file  │
   │  import     │         │   import    │         │  continuation│  │            │
   └─────────────┘         └─────────────┘         └─────────────┘   └─────────────┘
          │                       │                       │                │
          │                       │                       │                │
          └───────────────────────┴───────────────────────┴────────────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ BATCH SYSTEM    │
                         │ (UNICO FLUSSO)  │
                         └─────────────────┘
                                  │
                                  ▼
                    ┌──────────────────────────┐
                    │  Batch Orchestrator      │
                    │  ├─ Index & Filter       │
                    │  ├─ Queue Management     │
                    │  ├─ Batch Processing     │
                    │  └─ Auto Continuation    │
                    └──────────────────────────┘
                                  │
                                  ▼
                    ┌──────────────────────────┐
                    │  UNIFIED SESSION LOG     │
                    │  import-{session}.log    │
                    │  - All components log here│
                    │  - Complete trace        │
                    └──────────────────────────┘
```

**✅ VANTAGGI SITUAZIONE DESIDERATA:**
- UN SOLO flusso di import
- Tutti usano Batch System
- Log unificato per sessione
- Facile debug e monitoring
- Robusto e scalabile

---

## 📋 PIANO DI MIGRAZIONE

### FASE 1: Fix Critico (Immediato)
1. ✅ Rimuovere/commentare duplicato AJAX in `realestate-sync.php:106`
2. ✅ Rimuovere handler `handle_manual_import()` in `realestate-sync.php:535`
3. ✅ Test: Manual import usa solo Admin Class handler

### FASE 2: Migrare Scheduled Import
1. ✅ Modificare `run_scheduled_import()` per usare Batch Orchestrator
2. ✅ Rimuovere chiamata a `execute_chunked_import()`
3. ✅ Test: Scheduled import usa Batch System

### FASE 3: Migrare Test File Upload
1. ✅ Modificare `handle_import_test_file()` per usare Batch Orchestrator
2. ✅ Rimuovere chiamata a `execute_chunked_import()`
3. ✅ Test: Test upload usa Batch System

### FASE 4: Unificare Logging
1. ✅ Passare logger al Batch Orchestrator
2. ✅ Tutti i componenti loggano nel session log
3. ✅ Rimuovere log duplicati in debug.log

### FASE 5: Deprecare Chunked Import
1. ✅ Marcare `execute_chunked_import()` come @deprecated
2. ✅ Aggiungere warning se chiamato
3. ✅ Documentare migrazione

### FASE 6: Cleanup & Testing
1. ✅ Testare tutti i flussi
2. ✅ Verificare log unificato
3. ✅ Documentare nuovo flusso

---

**Fine Diagramma**
